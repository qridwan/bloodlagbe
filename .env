# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="mysql://root:password@localhost:3306/bloodlagbe_dev"

# A random string used to hash tokens, sign/encrypt cookies and generate cryptographic keys.
# You can generate one using: openssl rand -base64 32
# Example: NEXTAUTH_SECRET=your_super_secret_random_string_here
NEXTAUTH_SECRET=anysecret

# The canonical URL of your site. Required for production.
# In development, if your app runs on http://localhost:3000, set this:
NEXTAUTH_URL=http://localhost:3000
# For production deployment (e.g., on Vercel), update this to your production URL:
# NEXTAUTH_URL=https://yourdomain.com

# If adding Google Provider later:
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
